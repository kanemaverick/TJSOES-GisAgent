Metadata-Version: 2.4
Name: gis-agent
Version: 0.1.0
Summary: Natural-language GIS map generation agent with CRS guards and map standards.
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: openai>=1.75.0
Requires-Dist: geopandas>=1.0.1
Requires-Dist: shapely>=2.1.0
Requires-Dist: pyproj>=3.7.1
Requires-Dist: matplotlib>=3.10.1
Requires-Dist: matplotlib-scalebar>=0.9.0
Requires-Dist: folium>=0.19.5
Requires-Dist: contextily>=1.6.2
Requires-Dist: mapclassify>=2.8.1
Requires-Dist: pandas>=2.2.3
Requires-Dist: fiona>=1.10.1
Requires-Dist: pyogrio>=0.11.0
Requires-Dist: openpyxl>=3.1.5
Requires-Dist: scipy>=1.12.0

# GIS Agent

一个面向 ArcGIS Pro 风格执行路径的最小 GIS Agent 工程。

## 功能

- 自然语言任务转结构化 GIS workflow
- 固定工具注册表与确定性执行
- 强制 CRS / geometry 检查
- 标准静态地图输出
- 支持 Web 地图输出
- 无 API Key 时可走模板规划模式

## 快速开始

```bash
cd /home/xzkane/gis-agent
source .venv/bin/activate
pip install -e .
python examples/create_sample_data.py
gis-agent \
  --task "用示例行政区数据生成一张按 value 字段分级设色的地图，标题叫 Sample Map，输出 PNG" \
  --data examples/data/sample_polygons.geojson \
  --output-dir output/run1 \
  --mode template \
  --run
```

## LLM 模式

设置以下环境变量后可直接走大模型生成 workflow：

```bash
export OPENAI_API_KEY=...
export OPENAI_MODEL=gpt-4.1
```

兼容 OpenAI 风格接口：

```bash
export OPENAI_BASE_URL=https://your-compatible-endpoint/v1
```

## 输出

- `workflow_plan.json`: 结构化 GIS workflow
- `execution_report.json`: 执行报告
- `result.png` 或 `result.html`: 地图输出
